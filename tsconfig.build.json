{
  // Compiles src/ → dist/, which is what manifest.json and popup.html load.
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "types": ["chrome"],
    "noEmit": false,
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
