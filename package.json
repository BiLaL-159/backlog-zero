{
  "name": "backlog-zero",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && vite build && vite build --mode content",
    "watch": "rm -rf dist && (vite build --watch & vite build --mode content --watch & wait)",
    "typecheck": "tsc",
    "test": "node --test"
  },
  "devDependencies": {
    "@types/chrome": "^0.3.0",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "preact": "^10.29.8"
  }
}
